version: "3.7"

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.3.2
    container_name: zookeeper
    # hostname: zookeeper
    restart: always
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_SERVER_ID: 1
      ZOOKEEPER_SERVERS: zookeeper:2888:3888


  kafka:
    # image: confluentinc/cp-kafka:7.3.2
    image: datascientest/kafka
    # container_name: kafka
    # hostname: kafka
    restart: always
    ports:
      - "9092-9094:9092"
    depends_on:
      - zookeeper
    environment:
      KAFKA_ADVERTISED_HOST_NAME: 192.168.100.84
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      

  kafka_manager:
    image: hlebalbau/kafka-manager:stable
    container_name: kakfa-manager
    restart: always
    ports:
      - "9000:9000"
    environment:
      ZK_HOSTS: "zookeeper:2181"
      APPLICATION_SECRET: "random-secret"
    command: -Dpidfile.path=/dev/null


  kafka_ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    restart: always
    ports:
      - "8080:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: "kafka:9092"
      # KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: "192.168.100.84:9092,192.168.100.84:9093,192.168.100.84:9094"
      KAFKA_CLUSTERS_0_ZOOKEEPER: "zookeeper:2181"
    depends_on:
      - kafka
      - zookeeper





# docker-compose -f test.yml up -d

# Si je voudrais avoir deux brokers
# docker-compose -f test.yml up --scale kafka=3 -d